// import { fromJS } from 'immutable';
// import { makeSelectCartContainerDomain } from '../selectors';

// const selector = makeSelectCartContainerDomain();

describe('makeSelectCartContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
