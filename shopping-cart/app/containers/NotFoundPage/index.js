/**
 * NotFoundPage
 *
 * This is the page we show when the user visits a url that doesn't have a route
 *
 */

import React from "react";

const NotFound = () => {
  return <h1>The page you are looking for does not exist. Please go back.</h1>;
};
