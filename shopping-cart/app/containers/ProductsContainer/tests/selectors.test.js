// import { fromJS } from 'immutable';
// import { makeSelectProductsContainerDomain } from '../selectors';

// const selector = makeSelectProductsContainerDomain();

describe('makeSelectProductsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
